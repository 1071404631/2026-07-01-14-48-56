# 2026-07-02 工作日志

## 半岛医疗招聘 - 筛选条件提交系统

### 已完成
- 修复多选选项无法选中的 bug（label 默认行为冲突，改用 div + click 事件）
- 删除左上角半岛医疗 logo，只保留右上角
- 增加左侧职位列表，支持按提交人姓名加载、点击加载修改、删除
- 后端 API 增加按姓名查询（GET /api/submissions?name=xxx）和更新（PUT /api/submissions/:id）
- 修复刷新后左侧列表消失的问题（加入 localStorage 记住姓名）
- 编写 Render.com 免费部署指南（含 GitHub 上传指引）
- 编写 cpolar 免费内网穿透指南

### 待确认
- 用户正在尝试上传代码到 GitHub（GitHub Desktop 操作）
- 用户想部署到网上让同事填表，在选择免费方案（Render/cpolar/Vercel+Airtable）
- Vercel 免费版不支持 Node.js 后端，需要改造数据存储方式（Airtable/Google Sheets）

### 技术笔记
- 多选 bug 根因：<label> 的默认点击事件会干扰 JS 的 click 处理，改成 <div> 解决
- localStorage 键名：boss_submitter_name（记住提交人姓名）
- 后端 API_BASE 用 `window.location.origin` 自动适配本地和线上
